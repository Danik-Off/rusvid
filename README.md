<img src="assets/logo.svg" alt="" width="96" align="left" hspace="8" vspace="4">

# RusVid

**Учебный проект**: Android-приложение на React Native, которое показывает в
одном интерфейсе то, что Rutube, VK Видео и Sasflix отдают обычному браузеру.
Один поиск на три платформы, один плеер, одна история просмотров.

Написано, чтобы разобраться в трёх вещах: как держать несколько чужих API за
одним доменным контрактом, не пуская их поля в UI; как собрать плеер с
мини-режимом, очередью и жестами на `react-native-video`; и как довести bare
React Native до воспроизводимой сборки без Expo и облачных сервисов. Разбор
решений — в [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md).

> **Готовых сборок здесь нет.** Проект опубликован как исходный код: в релизах
> лежат заметки и тег, APK к ним не прикладывается. Приложение собирается
> локально — [docs/SETUP.md](docs/SETUP.md).
>
> **Приложение неофициальное и некоммерческое.** Не связано с Rutube, VK Видео
> и Sasflix, не одобрено ими и не является их продуктом; названия платформ
> принадлежат их владельцам и упомянуты только для указания совместимости.
> Оно не хранит и не раздаёт видео, ничего не скачивает, не снимает защиту и
> не обходит платный доступ. Полный текст — [DISCLAIMER.md](DISCLAIMER.md);
> он же показывается в приложении при первом запуске и живёт в настройках.

---

## Платформы

| Платформа | Вход | Что даёт вход | Поиск | Лента | Категории | Воспроизведение |
|---|---|---|---|---|---|---|
| **Rutube** | на сайте, в приложении | лента подписок | ✅ | ✅ | ✅ | нативно (HLS) |
| **Sasflix** | на сайте, в приложении | материалы по подписке | ✅ | ✅ | ✅ | нативно (HLS) |
| **VK Видео** | на сайте, в приложении | поиск по видео | ✅ | — | — | встроенный плеер VK |

Вход у всех трёх одинаковый: обычная форма входа самой платформы во
встроенном браузере, сессия остаётся в системном хранилище cookie Android.
**Регистрировать приложение, получать ключи и вводить какие-либо
идентификаторы не нужно ни для одной платформы. Пароль пользователя ни в
одном случае не проходит через код приложения.**

Rutube и Sasflix полностью работоспособны и анонимно. У VK анонимно работает
**воспроизведение** (встроенный плеер открывается по одному идентификатору
видео, без API и без сессии), а поиск требует входа — публичного поиска по
видео у VK нет. Обхода платного доступа, DRM или блокировок в проекте нет —
см. [docs/API-RESEARCH.md](docs/API-RESEARCH.md).

## Возможности

- **Единый поиск** — параллельный опрос всех включённых платформ, результаты
  перемешиваются round-robin, у каждой платформы своя пагинация.
- **Устойчивость к сбоям** — падение одной платформы не ломает выдачу
  остальных: сверху появляется полоса «VK: токен истёк», а результаты
  Rutube и Sasflix остаются на экране.
- **Лента и категории** — тренды всех платформ либо одна платформа со своим
  деревом категорий.
- **Плеер** — нативный ExoPlayer для HLS, WebView для embed, автоматический
  откат на веб-плеер платформы при сбое нативного и понятный текст ошибки
  вместо чёрного экрана. Свои элементы управления вместо системных:
  - **мини-плеер** — свайп вниз по кадру сворачивает видео в полоску над
    таб-баром, звук не прерывается, приложением можно пользоваться дальше;
    свайп вбок закрывает;
  - **очередь «Далее»** — список, из которого открыли видео, становится
    очередью: следующий ролик запускается сам, с отменяемым отсчётом;
  - **продолжение просмотра** — видео открывается с той секунды, на которой
    его закрыли, с кнопкой «Сначала»;
  - **жесты** — двойной тап по краю кадра перематывает (серия тапов
    складывается), удержание даёт 2× ускорение, вертикальный свайп в
    полноэкранном режиме меняет громкость и затемнение;
  - **качество, скорость, субтитры, звуковая дорожка** — из шторки, не
    прерывая воспроизведение;
  - **фоновое воспроизведение** с уведомлением, **картинка в картинке**,
    **таймер сна**, повтор и вписывание кадра.
- **Вход во все три платформы** — из настроек, без ввода пароля в приложении:
  OAuth у VK, форма самой платформы во встроенном браузере у Rutube и Sasflix.
- **Лента подписок** — появляется отдельной вкладкой, как только выполнен
  вход в платформу, которая её поддерживает.
- **Проверка платформ** — экран самодиагностики: живые запросы к API каждой
  платформы с таймингами и текстом ошибки на конкретном шаге.
- **Библиотека** — история просмотров с позицией воспроизведения и избранное,
  всё только на устройстве.

## Собрать себе

Готовых APK проект не выкладывает — сборка локальная:

```powershell
npm install
npm run android          # debug на подключённое устройство
npm run build:apk        # release APK: arm64-v8a (~20 МБ) и armeabi-v7a (~16 МБ)
```

Требования и установка тулчейна с нуля — [docs/SETUP.md](docs/SETUP.md).

> ⚠️ В `android/gradle.properties` есть блок `RNVideo_useExoplayerHls=true` —
> без него `react-native-video` собирается **без поддержки HLS**, и ни одно
> видео не играет, хотя сборка проходит успешно. Не удаляйте его.

## Структура

```
src/
├── app/          composition root, навигация, App
├── core/         доменная модель, контракт провайдера, агрегатор, ошибки
├── data/         HTTP-клиент, кэш, хранилище, репозитории
├── providers/    rutube / vk / sasflix — по каталогу на платформу
├── features/     экраны и их zustand-сторы
└── ui/           дизайн-токены и переиспользуемые компоненты
```

Зависимости идут строго сверху вниз: `core/` не знает про UI, `providers/`
не знает про экраны, UI не знает ни одного поля Rutube или VK.

## Документация

| Документ | О чём |
|---|---|
| [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) | слои, потоки данных, решения и компромиссы, подводные камни ExoPlayer |
| [docs/PROVIDERS.md](docs/PROVIDERS.md) | как добавить платформу; как устроена авторизация и вход в VK |
| [docs/API-RESEARCH.md](docs/API-RESEARCH.md) | разобранные эндпоинты трёх платформ с примерами ответов |
| [docs/SETUP.md](docs/SETUP.md) | локальная сборка, обязательные флаги, подпись APK, типичные проблемы |

## CI и версии

| Workflow | Когда | Что делает |
|---|---|---|
| [`ci.yml`](.github/workflows/ci.yml) | push в `main`, PR | `typecheck` → `lint` → юнит-тесты |
| [`android.yml`](.github/workflows/android.yml) | каждый push и PR | собирает debug APK, **проверяет наличие классов ExoPlayer HLS внутри APK**, кладёт артефакт на 14 дней |
| [`release.yml`](.github/workflows/release.yml) | **каждый push в `main` с изменением кода** | считает версию, коммитит её, ставит тег `v<version>` и публикует заметки |

APK нигде не публикуется: артефакт `android.yml` — это проверка того, что код
собирается, он живёт две недели и доступен только внутри Actions. Релиз — это
тег и заметки, файлов к нему не прикладывается. Почему так — [DISCLAIMER.md,
раздел 3](DISCLAIMER.md#3-некоммерческий-характер).

### Выпуск версии

Выпускать руками нечего: версия выходит сама на каждый push в `main`.

```powershell
git commit -am "feat: покадровая перемотка"
git push
```

CI считает версию по коммитам с последнего тега
([tools/next-version.mjs](tools/next-version.mjs), Conventional Commits):

| Коммиты с последнего тега | Шаг |
|---|---|
| `feat!: …`, `BREAKING CHANGE:` в теле | major (`1.2.3` → `2.0.0`) |
| `feat: …` | minor (`1.2.3` → `1.3.0`) |
| всё остальное (`fix`, `chore`, `ci`, …) | patch (`1.2.3` → `1.2.4`) |

Дальше CI сам записывает версию в `package.json`, прогоняет типы, линт и
тесты, коммитит `chore(release): v1.4.0 [skip ci]` обратно в `main`, ставит
тег `v1.4.0` и публикует заметки к нему. **После релиза нужен `git pull`** —
в `main` появился коммит от бота.

Ручные рычаги, когда автоматики мало:

```powershell
npm run version:next      # показать, какая версия выйдет, ничего не меняя
npm run release:minor     # задать версию явно — она главнее вычисленной
git commit -am "chore: v1.4.0" ; git push
```

- отдельный push без релиза — `[skip release]` в сообщении коммита;
- нужный шаг разово — Actions → Release → Run workflow → `bump`;
- правки только в `*.md` и `docs/` релиз не выпускают.

Версия локальной сборки берётся из того же `package.json`
([android/app/build.gradle](android/app/build.gradle)): `versionName` — как есть,
`versionCode` считается как `major*10000 + minor*100 + patch`, потому что
Android умеет сравнивать только целые. Тег и `versionName` разъехаться не
могут: тег ставится из той же строки, которую CI записал в `package.json`.
Существующий тег не перезаписывается.

Тег ставится последним — только после того, как прошли типы, линт и тесты:
тег на заведомо сломанном коммите хуже, чем его отсутствие.

### Почему проверяется HLS

Сборка без `RNVideo_useExoplayerHls=true` проходит успешно, но выдаёт APK,
в котором не играет ни одно видео. Поэтому `android.yml` падает и на потерянном
флаге, и на отсутствии классов в готовом `classes.dex` — проверяется
артефакт, а не только конфигурация. Ради этой проверки он и запускается на
`main`: релиз APK больше не собирает, и без неё потерянный флаг доехал бы до
следующей ручной сборки незамеченным.

## Разработка

```powershell
npm test           # 101 юнит-тест (без сети и нативных модулей)
npm run typecheck  # tsc --noEmit
npm run lint       # ESLint

# Живой смоук-тест: реальные запросы к Rutube и Sasflix
$env:RUSVID_LIVE = '1'; npm test -- live.smoke
```

### Логотип

Знак — треугольник «play», разрезанный на три дольки: три платформы, один
плеер. Вся фирменная графика порождается из одного описания геометрии:

```powershell
npm run icons
```

[tools/gen-icons.mjs](tools/gen-icons.mjs) перерисовывает `assets/logo.svg` и
`assets/logo-mark.svg`, векторные drawable и adaptive icon Android (включая
монохромный слой для тем Android 13+) и растровые mipmap-иконки для API 24–25,
где adaptive icons ещё не работают. Правьте генератор, а не его результат.
Тот же знак внутри приложения рисует
[src/ui/components/Logo.tsx](src/ui/components/Logo.tsx).

## Правовая рамка

Полный текст условий — [DISCLAIMER.md](DISCLAIMER.md), лицензия кода —
[LICENSE](LICENSE) (MIT; на контент платформ она никаких прав не даёт).
Ниже — суть.

**Приложение неофициальное и некоммерческое.** Автор — независимый
разработчик; проект не связан с Rutube, VK и Sasflix, не одобрен ими и не
выступает от их имени. Названия и обозначения платформ принадлежат их
владельцам и используются только для указания на совместимость: логотипы и
фирменная графика платформ не воспроизводятся, вся графика RusVid — своя.
Рекламы, покупок, подписок и пожертвований в приложении нет, автор не получает
с него дохода.

**Это исходный код, а не продукт.** Проект опубликован для разбора и учёбы;
готовых сборок автор не распространяет — ни в релизах, ни в магазинах
приложений. Кому нужно приложение, тот собирает его сам, и ответственность за
конкретную сборку лежит на том, кто её собрал и запустил.

**Приложение — клиент, а не сервис.** Своего сервера, каталога и базы видео у
него нет: поток идёт с серверов платформы прямо на устройство. Оно не
скачивает видео, не снимает DRM, не обходит подписки и не подменяет рекламу.
Платный контент показывается в списках с пометкой и открывается на сайте
платформы. Вход везде выполняется на странице самой платформы, приложение
работает от лица самого пользователя и его собственной сессии; **чужие
идентификаторы приложений (в том числе официальных клиентов платформ) не
используются**.

**Ответственность за использование — на пользователе.** Приложение
поставляется «как есть», без гарантий; неофициальный клиент может
противоречить пользовательскому соглашению платформы, и она вправе ограничить
доступ к аккаунту. Условия показываются при первом запуске: до нажатия
«Принимаю» приложение не делает ни одного сетевого запроса, а текст остаётся
доступен в «Настройки → О приложении → Правовая информация». Редакция условий
пронумерована (`LEGAL_VERSION` в
[src/core/legal/legalText.ts](src/core/legal/legalText.ts)) — при изменении
текста согласие спрашивается заново.

**Данные не покидают устройство.** Серверной части, аналитики, рекламных SDK и
телеметрии нет; история, избранное, настройки и сессии платформ хранятся
только в памяти телефона.

**Правообладателям и платформам.** Обращения — в
[issues](https://github.com/Danik-Off/rusvid/issues). По обоснованному
требованию поддержка платформы отключается или удаляется, а спорные сборки
снимаются с публикации. Судиться тут не за что: коммерческого интереса у
проекта нет.

Внутренние интерфейсы Rutube, Sasflix и веб-клиента VK не документированы
публично и могут измениться в любой момент — в этом случае правится только
маппер соответствующего провайдера.
